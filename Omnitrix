-- [[ OMNITRIX COMPLETO - BROOKHAVEN TEXT CLICK VERSION ]] --

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local lplayer = Players.LocalPlayer

-- === CONFIGURAÇÃO DE IDS ===
local IDS = {
    Omnitrix = "rbxassetid://110226612419601",
    Sons = {
        Carregado = "rbxassetid://6788288758",
        Ligando = "rbxassetid://6648679857",
        Trocar = "rbxassetid://3429876367",
        Transformar = "rbxassetid://74355094547165",
        Recarregar = "rbxassetid://6788288758",
        Descarregando = "rbxassetid://74043517398902"
    },
    Silhuetas = {
        [1] = {Nome = "Chama", ID = "rbxassetid://6052185947"},
        [2] = {Nome = "Quatro Braços", ID = "rbxassetid://7445053180"},
        [3] = {Nome = "XLR8", ID = "rbxassetid://13265535309"},
        [4] = {Nome = "Bala de Canhão", ID = "rbxassetid://12984933950"},
        [5] = {Nome = "Ultra T", ID = "rbxassetid://12995631685"},
        [6] = {Nome = "Diamante", ID = "rbxassetid://7444321735"},
        [7] = {Nome = "Fantasmatico", ID = "rbxassetid://12987202438"},
        [8] = {Nome = "Insectoide", ID = "rbxassetid://12995484781"},
        [9] = {Nome = "Massa Cinzenta", ID = "rbxassetid://13088356171"},
        [10] = {Nome = "Besta", ID = "rbxassetid://13256823140"},
        [11] = {Nome = "Aquatico", ID = "rbxassetid://15108826659"}
    }
}

-- === VARIÁVEIS DE CONTROLE ===
local currentAlienIndex = 1
local isOmnitrixOn = false
local isTransformed = false
local currentAlienActive = ""
local omnitrixCooldown = false
local clickCount = 0
local lastClickTime = 0
local activeLoops = {}
local originalGravity = workspace.Gravity

-- === FUNÇÃO DE ÁUDIO ===
local function playSound(soundId)
    local s = Instance.new("Sound", workspace)
    s.SoundId = soundId
    s.Volume = 1
    s.PlayOnRemove = true
    s:Destroy()
end

playSound(IDS.Sons.Carregado)

-- === INTERFACE GRÁFICA (GUI DO OMNITRIX) ===
local ScreenGui = Instance.new("ScreenGui", lplayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "OmnitrixSystem"
ScreenGui.ResetOnSpawn = false

local OmnitrixBtn = Instance.new("ImageButton", ScreenGui)
OmnitrixBtn.Size = UDim2.new(0, 110, 0, 110)
OmnitrixBtn.Position = UDim2.new(0.85, 0, 0.75, 0)
OmnitrixBtn.Image = IDS.Omnitrix
OmnitrixBtn.BackgroundTransparency = 1

local SelectionPanel = Instance.new("Frame", ScreenGui)
SelectionPanel.Size = UDim2.new(0, 200, 0, 200)
SelectionPanel.Position = UDim2.new(0.82, 0, 0.45, 0)
SelectionPanel.BackgroundTransparency = 1
SelectionPanel.Visible = false

local SilhouetteImg = Instance.new("ImageButton", SelectionPanel)
SilhouetteImg.Size = UDim2.new(0, 140, 0, 140)
SilhouetteImg.Position = UDim2.new(0.15, 0, 0.15, 0)
SilhouetteImg.BackgroundTransparency = 1
SilhouetteImg.ImageColor3 = Color3.new(0, 0, 0)

local NextBtn = Instance.new("TextButton", SelectionPanel)
NextBtn.Size = UDim2.new(0, 30, 0, 40)
NextBtn.Position = UDim2.new(0.85, 0, 0.4, 0)
NextBtn.Text = ">"
NextBtn.TextSize = 25
NextBtn.TextColor3 = Color3.new(1,1,1)
NextBtn.BackgroundTransparency = 0.5
NextBtn.BackgroundColor3 = Color3.new(0,0,0)

local PrevBtn = Instance.new("TextButton", SelectionPanel)
PrevBtn.Size = UDim2.new(0, 30, 0, 40)
PrevBtn.Position = UDim2.new(0, 0, 0.4, 0)
PrevBtn.Text = "<"
PrevBtn.TextSize = 25
PrevBtn.TextColor3 = Color3.new(1,1,1)
PrevBtn.BackgroundTransparency = 0.5
PrevBtn.BackgroundColor3 = Color3.new(0,0,0)

-- === SISTEMA DE CHAT (GRITO) ===
local function shoutInChat(text)
    local msg = string.upper(text) .. "!"
    if game:GetService("TextChatService").TextChannels:FindFirstChild("RBXGeneral") then
        game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync(msg)
    else
        pcall(function()
            ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
                :FindFirstChild("SayMessageRequest"):FireServer(msg, "All")
        end)
    end
end

-- === ENCONTRAR E CLICAR NO BOTÃO PELO TEXTO ===
local function clickButtonByText(targetText)
    -- Procura em toda a PlayerGui do jogador
    for _, gui in pairs(lplayer.PlayerGui:GetDescendants()) do
        -- Ignora a própria GUI do Omnitrix para não dar conflito
        if gui:IsA("TextButton") or gui:IsA("TextBox") then
            -- Verifica se o texto do botão é igual ao nome do alien (ignorando maiúsculas/minúsculas)
            if string.lower(gui.Text) == string.lower(targetText) then
                -- Força o clique simulando os eventos do botão
                local events = {"MouseButton1Click", "MouseButton1Down", "Activated"}
                for _, eventName in pairs(events) do
                    if gui[eventName] then
                        for _, connection in pairs(getconnections(gui[eventName])) do
                            connection:Fire()
                        end
                    end
                end
                break -- Já achou o botão, pode parar de procurar
            end
        end
    end
end

-- === LIMPEZA DE STATUS ANTERIORES ===
local function resetPlayerPhysics()
    for _, loop in pairs(activeLoops) do pcall(function() loop:Disconnect() end) end
    activeLoops = {}
    
    workspace.Gravity = originalGravity
    local char = lplayer.Character
    if char and char:FindFirstChildOfClass("Humanoid") then
        local hum = char:FindFirstChildOfClass("Humanoid")
        hum.WalkSpeed = 16
        hum.JumpPower = 50
        hum.UseJumpPower = true
    end
    
    if ScreenGui:FindFirstChild("BestaVision") then ScreenGui.BestaVision:Destroy() end
    if ScreenGui:FindFirstChild("CalcFrame") then ScreenGui.CalcFrame:Destroy() end
    if char and char:FindFirstChild("OriginalSize") then
        char.OriginalSize:Destroy()
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then pcall(function() v.Size = v.Size / 2 end) end
        end
    end
    for _, p in pairs(workspace:GetDescendants()) do
        if p:IsA("Highlight") and p.Name == "BestaESP" then p:Destroy() end
    end
end

-- === PULO INFINITO ===
local function enableInfiniteJump(speed, jPower)
    local char = lplayer.Character
    local hum = char:FindFirstChildOfClass("Humanoid")
    local jumpConn = UserInputService.JumpRequest:Connect(function()
        if char and hum and char:FindFirstChild("HumanoidRootPart") then
            hum.WalkSpeed = speed
            hum.JumpPower = jPower
            char.HumanoidRootPart.Velocity = Vector3.new(char.HumanoidRootPart.Velocity.X, jPower * 1.5, char.HumanoidRootPart.Velocity.Z)
        end
    end)
    table.insert(activeLoops, jumpConn)
end

-- === MECÂNICAS DOS ALIENS ===
local function applyAlienPowers(alienName)
    resetPlayerPhysics()
    local char = lplayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    
    if alienName == "Chama" then
        hum.WalkSpeed = 20
        hum.JumpPower = 75
        workspace.Gravity = originalGravity * 3
        
    elseif alienName == "Quatro Braços" then
        hum.WalkSpeed = 32
        hum.JumpPower = 100
        local tag = Instance.new("BoolValue", char) tag.Name = "OriginalSize"
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then pcall(function() v.Size = v.Size * 2 end) end
        end
        
    elseif alienName == "XLR8" then
        hum.WalkSpeed = 50
        hum.JumpPower = 50
        local xlr8Loop = RunService.Heartbeat:Connect(function()
            if hum.MoveDirection.Magnitude > 0 then
                if hum.WalkSpeed < 500 then hum.WalkSpeed = hum.WalkSpeed + 2 end
            else
                if hum.WalkSpeed > 50 then
                    hum.WalkSpeed = 1
                    if char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.Velocity = Vector3.new(0,0,0) end
                    task.wait(0.1)
                    hum.WalkSpeed = 50
                end
            end
        end)
        table.insert(activeLoops, xlr8Loop)
        
    elseif alienName == "Bala de Canhão" then
        hum.WalkSpeed = 10
        hum.JumpPower = 60
        local BallToggle = Instance.new("TextButton", ScreenGui)
        BallToggle.Size = UDim2.new(0, 100, 0, 40)
        BallToggle.Position = UDim2.new(0.85, 0, 0.35, 0)
        BallToggle.Text = "Modo Bola: OFF"
        BallToggle.BackgroundColor3 = Color3.fromRGB(85, 170, 0)
        local modeOn = false
        BallToggle.MouseButton1Click:Connect(function()
            modeOn = not modeOn
            if modeOn then
                BallToggle.Text = "Modo Bola: ON"
                hum.WalkSpeed = 60; hum.JumpPower = 0
            else
                BallToggle.Text = "Modo Bola: OFF"
                hum.WalkSpeed = 10; hum.JumpPower = 60
            end
        end)
        table.insert(activeLoops, {Disconnect = function() BallToggle:Destroy() end})

    elseif alienName == "Ultra T" then
        hum.WalkSpeed = 20
        hum.JumpPower = 60
        workspace.Gravity = originalGravity / 6
        
    elseif alienName == "Diamante" then
        hum.WalkSpeed = 25
        hum.JumpPower = 70
        
    elseif alienName == "Fantasmatico" then
        hum.WalkSpeed = 26
        hum.JumpPower = 60
        workspace.Gravity = originalGravity / 2
        enableInfiniteJump(50, 10)
        
    elseif alienName == "Insectoide" then
        hum.WalkSpeed = 20
        hum.JumpPower = 75
        workspace.Gravity = originalGravity / 2.5
        enableInfiniteJump(75, 20)
        
    elseif alienName == "Massa Cinzenta" then
        hum.WalkSpeed = 12
        hum.JumpPower = 55
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then pcall(function() v.Size = v.Size / 4 end) end
        end
        local CalcFrame = Instance.new("Frame", ScreenGui)
        CalcFrame.Size = UDim2.new(0, 150, 0, 150)
        CalcFrame.Position = UDim2.new(0.1, 0, 0.4, 0)
        CalcFrame.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
        local CalcTxt = Instance.new("TextLabel", CalcFrame)
        CalcTxt.Size = UDim2.new(1, 0, 0.3, 0); CalcTxt.Text = "Massa Cinzenta Inteligência"; CalcTxt.TextColor3 = Color3.new(1,1,1); CalcTxt.TextScaled = true
        local CalcBtn = Instance.new("TextButton", CalcFrame)
        CalcBtn.Size = UDim2.new(0.8, 0, 0.5, 0); CalcBtn.Position = UDim2.new(0.1, 0, 0.4, 0); CalcBtn.Text = "Calcular"
        CalcBtn.MouseButton1Click:Connect(function() CalcTxt.Text = "Resultado: " .. tostring(math.random(1000, 99999)) end)
        table.insert(activeLoops, {Disconnect = function() CalcFrame:Destroy() end})
        
    elseif alienName == "Besta" then
        hum.WalkSpeed = 40
        hum.JumpPower = 65
        local BestaVision = Instance.new("Frame", ScreenGui)
        BestaVision.Size = UDim2.new(1, 0, 1, 0); BestaVision.BackgroundColor3 = Color3.new(0,0,0)
        local visionLoop = task.spawn(function()
            while true do
                task.wait(math.random(5, 10))
                BestaVision.BackgroundTransparency = 0.8
                task.wait(0.3)
                BestaVision.BackgroundTransparency = 0
            end
        end)
        table.insert(activeLoops, {Disconnect = function() task.cancel(visionLoop); BestaVision:Destroy() end})
        local espLoop = RunService.Heartbeat:Connect(function()
            if not char:FindFirstChild("HumanoidRootPart") then return end
            for _, p in pairs(workspace:GetDescendants()) do
                if p:IsA("Humanoid") and p.Parent ~= char and p.Parent:FindFirstChild("HumanoidRootPart") then
                    local targetHRP = p.Parent.HumanoidRootPart
                    local dist = (char.HumanoidRootPart.Position - targetHRP.Position).Magnitude
                    if dist <= 100 then
                        if not p.Parent:FindFirstChild("BestaESP") then
                            local hl = Instance.new("Highlight", p.Parent)
                            hl.Name = "BestaESP"; hl.FillColor = Color3.new(1, 0, 0); hl.OutlineColor = Color3.new(1, 0, 0)
                        end
                    else
                        if p.Parent:FindFirstChild("BestaESP") then p.Parent.BestaESP:Destroy() end
                    end
                end
            end
        end)
        table.insert(activeLoops, espLoop)
        
    elseif alienName == "Aquatico" then
        hum.WalkSpeed = 15
        hum.JumpPower = 55
        local timerForaDaAgua = 0
        local aquaticoLoop = RunService.Heartbeat:Connect(function()
            if not char:FindFirstChild("HumanoidRootPart") then return end
            local naAgua = false
            local parts = workspace:GetPartsInPart(char.HumanoidRootPart)
            for _, part in pairs(parts) do
                if part.Color.B > 0.6 and part.Color.G < 0.5 and part.Color.R < 0.5 then
                    naAgua = true
                    break
                end
            end
            if naAgua then
                hum.WalkSpeed = 75; hum.JumpPower = 55; timerForaDaAgua = 0
            else
                timerForaDaAgua = timerForaDaAgua + task.wait()
                if timerForaDaAgua >= 120 then
                    hum.WalkSpeed = 0; hum.JumpPower = 0
                else
                    hum.WalkSpeed = math.max(0, 15 - (timerForaDaAgua * 0.12))
                end
            end
        end)
        table.insert(activeLoops, aquaticoLoop)
    end
end

-- === LÓGICA DE TRANSFORMAÇÃO ===
local function transform(alienName)
    if omnitrixCooldown then return end
    isTransformed = true
    currentAlienActive = alienName
    SelectionPanel.Visible = false
    
    playSound(IDS.Sons.Transformar)
    shoutInChat(alienName)
    
    -- Clica automaticamente no botão que tem o texto com o nome do Alien!
    clickButtonByText(alienName)
    applyAlienPowers(alienName)
    
    local timeActive = math.random(120, 360)
    task.wait(timeActive - 5)
    
    playSound(IDS.Sons.Descarregando)
    task.wait(5)
    
    isTransformed = false
    currentAlienActive = ""
    resetPlayerPhysics()
    
    -- Clica automaticamente no botão com o texto "Ben" para resetar a skin
    clickButtonByText("Ben")
    
    omnitrixCooldown = true
    OmnitrixBtn.ImageColor3 = Color3.new(1, 0, 0)
    task.wait(480)
    
    omnitrixCooldown = false
    OmnitrixBtn.ImageColor3 = Color3.new(1, 1, 1)
    playSound(IDS.Sons.Recarregar)
end

-- === CLIQUE NO RELÓGIO / SPAM DETECT ===
local function updateSilhouette()
    SilhouetteImg.Image = IDS.Silhuetas[currentAlienIndex].ID
end

OmnitrixBtn.MouseButton1Click:Connect(function()
    if omnitrixCooldown or isTransformed then return end
    
    local now = os.clock()
    if now - lastClickTime < 0.4 then
        clickCount = clickCount + 1
    else
        clickCount = 1
    end
    lastClickTime = now
    
    if clickCount >= 4 then
        local randomAlien = IDS.Silhuetas[math.random(1, #IDS.Silhuetas)].Nome
        transform(randomAlien)
        clickCount = 0
        return
    end
    
    isOmnitrixOn = not isOmnitrixOn
    if isOmnitrixOn then
        playSound(IDS.Sons.Ligando)
        SelectionPanel.Visible = true
        updateSilhouette()
    else
        SelectionPanel.Visible = false
    end
end)

NextBtn.MouseButton1Click:Connect(function()
    if not isOmnitrixOn then return end
    playSound(IDS.Sons.Trocar)
    currentAlienIndex = currentAlienIndex + 1
    if currentAlienIndex > #IDS.Silhuetas then currentAlienIndex = 1 end
    updateSilhouette()
end)

PrevBtn.MouseButton1Click:Connect(function()
    if not isOmnitrixOn then return end
    playSound(IDS.Sons.Trocar)
    currentAlienIndex = currentAlienIndex - 1
    if currentAlienIndex < 1 then currentAlienIndex = #IDS.Silhuetas end
    updateSilhouette()
end)

SilhouetteImg.MouseButton1Click:Connect(function()
    if not isOmnitrixOn or isTransformed then return end
    local chosenAlien = IDS.Silhuetas[currentAlienIndex].Nome
    transform(chosenAlien)
end)

-- === AUTO-DESTRUIÇÃO ===
lplayer.Chatted:Connect(function(msg)
    if string.lower(msg) == "ativar modo de auto-destruição" then
        if omnitrixCooldown then return end
        
        isOmnitrixOn = false
        SelectionPanel.Visible = false
        omnitrixCooldown = true
        OmnitrixBtn.ImageColor3 = Color3.new(1, 0, 0)
        
        local TimerLbl = Instance.new("TextLabel", ScreenGui)
        TimerLbl.Size = UDim2.new(0, 200, 0, 50); TimerLbl.Position = UDim2.new(0.4, 0, 0.1, 0)
        TimerLbl.TextColor3 = Color3.new(1,0,0); TimerLbl.TextSize = 40
        
        for i = 20, 1, -1 do
            TimerLbl.Text = "AUTO-DESTRUIÇÃO: " .. i
            playSound(IDS.Sons.Descarregando)
            task.wait(1)
        end
        
        TimerLbl:Destroy()
        resetPlayerPhysics()
        ScreenGui:Destroy()
        
        local hrp = lplayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.Velocity = Vector3.new(math.random(-500, 500), 8000, math.random(-500, 500))
            hrp.RotVelocity = Vector3.new(500, 500, 500)
        end
    end
end)
